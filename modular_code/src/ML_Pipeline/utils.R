##----------------------------------------------------------------
##                Function to load data from CSV                --
##----------------------------------------------------------------
loadSourceData <- function(path_to_data){  
  return(read.csv(path_to_data))
}
